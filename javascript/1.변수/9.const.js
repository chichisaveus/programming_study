// let은 재할당이 가능했음

let a = 1;
a = 2;

// const는 재할당이 불가능함

// const는 일종의 '상수' 역할을 한다고 생각하면 됨.

const text = 'hello'; // 이렇게 설정되면 재할당이 불가능함.
// text = 'hi' 라고 할수없음

// 재할당이 불필요한 경우
const apple = {
  name: '사과',
  color: 'red',
};
// 이렇게 선언하면, apple이 가리키는 '주소'가 상수로 설정되고, heap에 저장된 객체 내용 자체는 상수가 아님
//즉 다시말하면
apple.name = 'orange'; // 이렇게 바꿀수가 있다. 객체 내용은
//그러나
//apple = orange;  // 이런식으로 주소자체를 다른곳으로 재할당은 불가능함. 이해감??
