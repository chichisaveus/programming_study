//  주석을 달면됨

/**
 * 주석이됨
 * 다음줄도됨
 * 계쏙 됨
 *
 */

/**
 * 변수규칙:
 * 라틴문자(0-9, a-z, A-Z), _
 * 대소문자 구분함
 * 한국어, 예약어 사용 안됨.
 * 숫자로 시작 x
 * 특수문자 사용 x ( _,$ 두개는 예외)
 */

// 예시 들어간다.

let apple;
let redApple; // 단어두개를 연결할시엔 대문자를 넣어서 구분한다.

// 나쁜예제
let number = 20;
let audio1;
let audio2; // 이런식으로 숫자 넣는 방식

// 좋은예제
let myAge = 20;
let audioBackground;
let audioWind; // 이런식으로 묶음으로 쓰면 audio만 검색하면 관련 변수들이 딱 묶여서 검색된다.  큰그룹, 그뒤에 더 작은그룹 , 더작은그룹 이런식으로 변수를 넣어라.
